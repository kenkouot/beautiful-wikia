/**
 * module.js
 * @description ngModule to bind services to and load as ngApp dependency
 */
define([ 'angular' ], function( angular ) {
  'use strict';
  return angular.module( 'wikia.services', [] );
});
